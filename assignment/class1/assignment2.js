/**

@param {string} dirPath
@param {string} extension e.g. ".js"
@returns {string[]} sorted absolute file paths
*/
function findFilesByExtension(dirPath, extension) {
  // TODO: validate dirPath exists, then recursively collect matches
}

module.exports = { findFilesByExtension };
